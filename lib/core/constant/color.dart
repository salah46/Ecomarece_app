import 'package:flutter/material.dart';

class Appcolor {
  static Color white = Colors.white;

  static Color black = const Color.fromRGBO(31, 31, 31, 100);
  static Color purple = const Color.fromRGBO(
      127, 39, 255, 1.0); // Represents a primary purple color
  static Color sereneBlue = const Color.fromRGBO(
      159, 112, 253, 1.0); // Evokes a sense of serenity with a blue tone
  static Color goldenSunset = const Color.fromRGBO(
      253, 191, 96, 1.0); // Resembles the warm hues of a golden sunset
  static Color vibrantOrange = const Color.fromRGBO(255, 137, 17, 1.0);
  static Color yellow = Colors.yellow; 
  static Color warmPearlGray = Color.fromARGB(255, 245, 243, 243);// Represents a vibrant orange color
}
